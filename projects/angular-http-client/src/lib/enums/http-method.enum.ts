/**
 * Protocolo HTTP utilizado nas requisições.
 */
export const enum HttpMethod {
  Get = 'get',
  Post = 'psot',
  Put = 'put',
  Head = 'head',
  Delete = 'delete',
  Patch = 'patch',
  Options = 'options',
  Connect = 'connect',
  Trace = 'trace',
}
